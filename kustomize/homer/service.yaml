apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: homer
  labels:
    app: homepage
spec:
  selector:
    app: homepage
  ports:
    - name: http
      port: 3000
      targetPort: 3000
