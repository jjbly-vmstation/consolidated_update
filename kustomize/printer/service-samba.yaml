apiVersion: v1
kind: Service
metadata:
  name: cups-nodeport
  namespace: print
spec:
  selector:
    app: cups-avahi
  type: NodePort
  ports:
    - name: ipp
      port: 631
      targetPort: 631
      nodePort: 30631
      protocol: TCP
